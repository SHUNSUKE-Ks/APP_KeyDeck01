[package]
name = "proto-adapter-win"
version = "0.1.0"
edition = "2021"

[dependencies]
proto-keymap = { path = "../proto-keymap" }

[target.'cfg(windows)'.dependencies]
windows = { version = "0.62.2", features = ["Win32_UI_Input_KeyboardAndMouse", "Win32_Foundation"] }
